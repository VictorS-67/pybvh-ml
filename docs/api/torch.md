# PyTorch

Optional — requires `pip install "pybvh-ml[torch]"`. See the [PyTorch Integration guide](../guide/pytorch.md) for the training-loop context, the `set_epoch` contract, and the `length` semantics.

## Datasets

::: pybvh_ml.torch.datasets
    options:
      members_order: source
      show_root_heading: true

## Collate

::: pybvh_ml.torch.collate
    options:
      members_order: source
      show_root_heading: true
